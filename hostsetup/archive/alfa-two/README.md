# Abandoned alfa-two host artifacts

These files are retained only as T-009 audit and rollback evidence for the
abandoned `alfa-two` deployment target. They contain exact, machine-specific
PCI IDs and BDFs for its faulty RTX A2000 path.

Do not install either file on the active `ar-Default-string` rt-control host.
That host has a different healthy RTX 2000 Ada and requires no PCI quarantine.

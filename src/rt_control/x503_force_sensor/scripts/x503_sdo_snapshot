#!/usr/bin/env python3
"""Retired runtime SDO entry point: never accesses EtherCAT."""
import sys

print("X503 parameters are initialized by RT-Control in PREOP; standalone SDO polling is disabled.", file=sys.stderr)
raise SystemExit(2)

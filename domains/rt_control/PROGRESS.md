# rt-control implementation progress

| Task | Commit | Self-check result | Remaining issues |
| --- | --- | --- | --- |
| Joint5 reverse correction | source: this commit | PASS — `left_joint5` and `right_joint5` now use dedicated ZeroErr profiles that negate command/state position in the EtherCAT master coordinate layer; shared `zeroerr_j5.yaml` remains unreversed. `right_joint4` remains reversed via its dedicated profile. | Requires separately authorized, supervised low-speed direction checks. This code change does not authorize enable or motion. |
| ZeroErr position transform request | `4dfdc03cecd5f93d89e73618acf3b2de457cb135`, `61082c60e280ced4f09aee8e16baef094dd6bd0c`, plus this correction | PASS — `right_joint4`, `right_joint5`, and `left_joint5` use dedicated profiles that negate only position command/state; no `0x607E` is written and shared `zeroerr_j4.yaml`/`zeroerr_j5.yaml` remain unchanged. Profile-isolation and ±90° transform tests, Xacro expansion, `robot_hw_ethercat` build/CTest and repository gate checks pass. | Requires separately authorized, supervised low-speed direction checks. This code change does not authorize enable or motion. |
| Direction polarity request | source: this commit | PASS — `right_joint2` (ring position 2) and `left_joint3` (ring position 9) now use dedicated Ti5 profiles with startup `0x607E:00 uint8 = 0x80`; shared `ti5_j2.yaml`/`ti5_j3.yaml` remain unchanged, so `left_joint2`/`right_joint3` retain their original behavior. New profile-isolation tests, Xacro expansion, `robot_hw_ethercat` build/CTest and repository gate checks pass. | Requires a separately authorized, supervised hardware readback plus low-speed direction check. This code change does not authorize SDO writes, enable, or motion. |
| T-002 | `7d9a6b259de79902b65d2445eb793a29a3fb3dac` | PASS — `colcon build --symlink-install` (8 packages); workflow/deps YAML parse; shell syntax; `diff_legacy.py --help` | CI has not run remotely; migration gate remains an explicit T-005 placeholder. |
| T-001 | `a18ae1fc5583d2e9b93f6ebfe805a8121aabe004` | PASS — verified all five HEAD/status values, authoritative worktree clean at `6bc94cd`, config `diff -rq`, full-depth CANopen search, SHA-256 inventory, and report required sections | BQ-001 blocks T-005 gate policy; BQ-002 blocks T-010 preload safety closure. |
| T-006 | `b551dcf3bb6770390fcbbf1ef9e423336caed6df` | PASS — 8-package `colcon build`; bus YAML schema/exact-scale/no-write-key assertions; `bash -n`; archive API grep is CORead-only; EDS byte SHA-256 matches source | BQ-003/004/005/006 block CANopen activation and some T-007/T-011/T-012 wiring; live SDO archive and generated DCF inspection remain T-014 commissioning work. |
| T-004 | `ad5e295545f57b11ce7f57d410b8dc53dda6ede3` | PASS — eight-profile YAML schema and Ti5 CSP overlay checks; `joint_limits.yaml` exact-text `cmp`; 13-axis xacro expansion/interface mapping; `diff_legacy.py` 100% (761 semantic values); `colcon build --symlink-install --packages-select robot_hw_ethercat rt_control_bringup` | Live ring identity, drive watchdog registers/reaction, preload behavior, and low-speed direction/scale remain gated commissioning work in T-013/T-015. |
| T-005 | `069c7f0305e52cb2df4116ce6f53f47733328f2e` | PASS — gate reports 100% across 13 logical-axis profiles and 761 semantic values; a one-digit float mutation returns exit 1 with both affected shared-profile axes; `py_compile`; CI YAML parse | Remote CI has not run; its frozen-baseline checkout requires repository access from the CI runner. |
| T-003 | `9e9e4bbd5a82249d9e3a35dd71bb71f55f32fb20` | PASS — exact source commit verified; 42 mesh files byte-identical; `xacro` expansion and `check_urdf` pass; bus/`ros2_control` grep empty; package-only `colcon build` passes | No SRDF exists in the user-designated source subtree, so none was invented; hardware xacros remain bringup-owned. |
| T-008 | `b29db80132701f66139d329da29e4d5ebb8ce5f6` | PASS — clean detached-worktree image build tagged with the exact commit; Tsinghua Ubuntu/ROS/rosdistro sources; IgH userspace build reports `1.6.10`; 8-package image build, `docker compose config`, shell syntax, and container package checks pass | Host-side `ethercat version` comparison and validated CPU set remain T-009 hardware-host work. |
| T-011 | `67204e94cfc4f84ab6305ce69a631d9056f48545` | PASS — `colcon list --names-only` confirms `rt_watchdog` is absent; standalone watchdog package removed exactly as adjudicated | CAN heartbeat/EMCY handling now depends on the upstream ros2_canopen/Lely path plus the approved narrow track-EMCY group-stop overlay; powered proof remains T-014. |
| T-010 | `12097c5297ce6066caea157e237942d1c84bdd9b` | PASS — full production image build; mock `/rt/enable` deterministically reports batch 0, `right_joint1`, statusword 64 and `enable_batch_timeout`; `/rt/reset_fault` returns diagnostics to IDLE; ICube patch applies cleanly at its pinned commit | Real 13-axis staged enable, raw-position preload/no-jump behavior and physical quick-stop/disable terminal states remain T-013 hardware evidence. |
| T-007 | `626b29fd2618ce6698e867f91eec635a40d98b02` | PASS — production xacro expands and passes `check_urdf`; mock loads JSB/updown/diff-drive ACTIVE, JTC INACTIVE and enable manager ACTIVE; JTC rejects 0.02 rad and succeeds at 0.01 rad; updown moves to 0.1 m then rejects a 0.1 m start mismatch | Real CANopen NMT/PDO/EMCY, paired-track stop and real-drive updown PP behavior remain T-013/T-014 commissioning evidence. |
| T-012 | `2d31502ef20e78445d1dd55885962a7cb2c6096d` | PASS — mock `/diagnostics` reports EtherCAT master and slaves 1..12/14 OK with SwitchOnDisabled, and CANopen nodes 1/2/3 STALE without fabricated traffic | Native ros2_canopen diagnostic fields and physical rqt/1 Hz observation remain T-014/T-015 hardware evidence. |
| T-008 (final corrective) | `ec09a55cdfb627304923a48a63374e72a9f1304e` | PASS — Docker build 17/17; Compose validates; IgH reports 1.6.10; runtime/history contain no proxy; PID 1 is direct `rt_control_start`; `docker stop` logs `/rt/disable ok=true`, orderly ROS/hardware shutdown and exits 0 | Host IgH parity and validated production CPU affinity remain T-009; real-drive shutdown terminal state remains T-013. |
| T-013 (BQ-114 migration audit) | `0b303779c38223b6fae0e1725d9cd055d5be975b` | PASS — `diff_legacy.py` reports 100% across 13 logical-axis profiles and `joint_limits.yaml` (813 semantic values); unit tests verify the approved `0x10F1:02=100` ordering/type overlay and reject an unapproved value mutation | This audit encodes the already approved BQ-114 tolerance; low-speed motion, Turn jog, long-duration DC stability, and the complete production-stack exit remain T-013/T-015 commissioning work. |
| T-GOV-001 | `a2bff26b030d94a7ae1d43501d652b425432e2d7` | PASS — `tools/quality_gate.sh` validates repository architecture/file hygiene, runs 22 policy tests at 84% branch coverage, checks shell syntax, and enforces the EtherCAT sync/shutdown policy; direct `py_compile` and `bash -n` also pass | ShellCheck is unavailable locally and is mandatory in remote CI; GitHub branch protection still requires an administrator after the first remote workflow succeeds. |
| T-GOV-002 | `8783e32227b7a6f2c655a81a8db0cb43dde82503` | PASS — root README/AGENTS and PR contract now describe the five-domain monorepo; rt-control README/AGENTS/progress/blockers and five evidence documents live under `domains/rt_control`; 22 policy tests, 84% branch coverage, local-link checks, stale-marker checks, and the domain-ledger placement policy pass | ShellCheck remains unavailable locally; remote CI still requires a `main` push or pull request because the workflow does not run on feature-branch pushes. |
| T-DOC-001 | `038cf35f5ff7efb1360eaf38a1ef97559c27b998` | PASS — added the Chinese onboarding knowledge map and staged new-host deployment/operations runbook; cross-checked source, runtime configuration, adjudicated BQs and commissioning evidence; `git diff --check`, local Markdown target checks and `tools/quality_gate.sh` pass | Documentation does not close the recorded Updown remap, RMW/DDS, Ti5 BQ-115/BQ-117, T-009 soak, full-stack exit, release automation or hardware-motion acceptance gaps; no host, container, bus or powered-hardware action was performed. |
| T-016 | `84a4e909cc2a304e837dcd4747d7b82feb2fa72a` | PASS — generated the XMC HW 1.0 / SW 5.11 profile from the live slave-15 SII; YAML parsing and explicit bit-sum assertions confirm fixed RxPDO `0x1600` is 7 entries/19 bytes and TxPDO `0x1A00` is 10 entries/24 bytes; identity, scaling and stale-XML exclusion assertions plus `repository_gate.py` and `git diff --check` pass | Profile is deliberately not connected by T-016 alone. No SDO write, OP transition, enable or motion was performed; runtime integration and hardware gates remain T-017/T-019. |
| T-017 | `95527cc88700a50a90e193ac9afa63a00d05e907` | PASS — integrated XMC Updown as EtherCAT position 15 and the 14th JTC/enable axis; `diff_legacy.py` matches 14 profiles and limits (921 semantic values), 23 policy tests and the repository gate pass, both upstream patches apply to their pins, affected local packages and an isolated patched JTC build pass; clean image `rt-control:95527cc88700a50a90e193ac9afa63a00d05e907` builds 22 packages, contains only CAN Node 2/3, has no legacy Updown interface/controller artifacts, expands the production URDF, and its no-device Mock exposes/claims all 14 EtherCAT controlword/statusword pairs; FJT Mock rejects Updown start error `0.06 m` and accepts `0.04 m` at the frozen `0.05 m` threshold | No real XMC startup SDO, OP transition, preload, fifth-batch enable, CSP motion, fault/reset or shutdown test was performed. The supplied XML remains incompatible with live SW 5.11 fixed PDO; T-019 hardware commissioning is mandatory. |
| T-019 (partial) | `c5ed7f0fa78ccdad4d23f1116277bcf0642d93dc` | PARTIAL PASS — on `ar@192.168.0.40`, live identity and the 19/24-byte XMC profile reached 16-slave OP/WC-complete; expected Ti5 `left_joint3 0x1208` was cleared by one successful group reset; one 14-axis enable returned success, JTC became active, slave 15 reached OperationEnabled and Updown changed only one count; one group disable returned success and slave 15 returned SwitchOnDisabled; final CAN heartbeats were Stopped and EtherCAT master inactive/all 16 PREOP; `tools/quality_gate.sh` still passes | BLOCKED — OP lost frames grew 391→399 and WC errors 705→712 with repeated 3/4-datagram timeouts (BQ-120); CANopen deactivate raced a polling callback and crashed `ros2_control_node` in `LelyDriverBridge::get_id()` before orderly EtherCAT deactivate, while the container misleadingly exited 0 (BQ-119); missing BMS adapter required a one-run, identity-checked systemd dependency exception (BQ-121). No FJT or velocity command was sent; further powered enable/motion is prohibited until BQ-119/BQ-120 are closed. |
| T-019 (BQ-119/BQ-120/BQ-122 corrective) | `4fc8414f67b63bf3a1c4fb4c34eb27fe8caafc9d` | PASS — upstream ros2_canopen test target reports 17/17; affected local packages and clean 22-package Docker image build pass; all 14 drive slaves read back `0x10F1:02=250`; three complete hardware start/stop cycles order disable/controller quiesce/EtherCAT inactive before CANopen executor and driver cleanup, container/`ros2_control_node` exit cleanly with no UAF, `0x001B` or datagram timeout; one cycle also passed group reset/enable/hold/disable without motion; IgH lost frames remained 399→399; final master Idle/Inactive and all 16 slaves PREOP | BQ-119/BQ-122 are closed. BQ-120 is risk-accepted and localized by CRC/PHY counters to `slave 1 port 1 -> slave 2 port 0`, but cable/connector/port repair and 30-minute soak remain; unrecorded IgH `run_on_cpu=12` is a secondary scheduling risk. BQ-117 width conflict and BQ-121 missing-adapter production policy remain open. No FJT, `/cmd_vel` or fault/EMCY injection was performed. |
| T-019 (14-axis minimal FJT) | `4fc8414f67b63bf3a1c4fb4c34eb27fe8caafc9d` | PARTIAL PASS — hardware E-stop/collision preconditions were operator-confirmed; 28/28 complete 14-axis FJT segments executed one axis at a time, with each rotary axis moving out/back `0.5 degree` and Updown out/back `0.05 m` at `2 s` per segment; maximum moving-axis tracking error was `0.162928902 degree` and Updown `0.00184546718425 m`; FIFO/80 CPU14 averaged 4.945% busy over the 180 s performance window; `/rt/disable`, CANopen/EtherCAT orderly shutdown and container exit 0 passed; final master was Idle/Inactive, all 16 slaves PREOP and both CAN links had zero errors; evidence manifest verification and `tools/quality_gate.sh` pass | Not a full T-019 close: BQ-120 recurred with lost frames 399→402, slave-2 input CRC 8→11 and three 4-datagram timeout events (one during FJT), though no drive/action failure occurred under the approved 1000 ms tolerance. No simultaneous-axis trajectory, rejection/fault/EMCY/断链 injection, 30-minute soak or recorded onsite physical-direction/noise observation. Evidence: `/var/lib/rt-control/validation/run-18-fjt-low-speed-4fc8414/`. |
| T-DOC-002 | runtime `4fc8414f67b63bf3a1c4fb4c34eb27fe8caafc9d` | PASS — added separate deployment/performance, integration-readiness and one-command-start documents; the current-IPC launcher locks host/kernel/CPU/bus/image identity, requires an interactive hardware authorization before startup, automatically calls `/rt/enable`, and disables/stops on incomplete startup; `bash -n`, help/host-lock checks, non-Git release-wrapper positive/negative checks, launcher policy gate and `tools/quality_gate.sh` pass | The new combined launcher has not been deployed or run against powered hardware; rt-control owner must complete one controlled start→READY→stop first run before colleagues use it. Current engineering ROS interfaces remain provisional pending the shared inter-domain contract and safety-diagnostics redesign; BQ-120 physical-link work remains. |
| T-020 | source: this commit; Mock base: `4fc8414f67b63bf3a1c4fb4c34eb27fe8caafc9d` | PASS — test-first TF contract reproduced three expected failures then passed 3/3; Xacro/check_urdf gives sole root `base_footprint`; affected packages build; no-device container Mock reports `/tf` and `/tf_static` as `tf2_msgs/msg/TFMessage`, publishes `odom→base_footprint` plus static `base_footprint→base_link` at exact 0.202094 m, resolves composed `odom→base_link`, and runtime edge inspection contains no `world`; isolated `tools/quality_gate.sh` passes 26 tests at 84% coverage | New production image is not built/deployed and target IPC was not accessed; current one-command launcher still locks `4fc8414…`. A rebuilt image, release/launcher lock update and target-host read-only TF check remain before navigation/vision integration. Local symlink-install requires invoking `rt_control_start` through bash because its source file lacks the executable bit; the ordinary Docker install is executable. |
| T-RT-IO-001 | `e4fed685bfa4485c210ad038c804a331b4801d88` | PASS — selectively integrated only `bms_node` and `plc_node`; removed duplicate command topics and `can_bus_guard`; added typed `PlcIoState`, confirmed single-bit/readback/remote-enable semantics including full-word `%MW201` readback, central config and same-container launch; affected four-package build, 47 package tests, Compose expansion, launch argument parsing, 17-file flake8, focused 86% branch coverage and `tools/quality_gate.sh` (26 tests, 84% coverage) pass | No PLC/CAN/actuator access or target-IPC run was performed in this stage. PLC/BMS temporarily share CPU14 by explicit risk acceptance. Clean image, Mock and release-lock evidence are recorded separately in T-RT-IO-002; controlled IO commissioning remains before use. |
| T-RT-IO-002 | source `e4fed685bfa4485c210ad038c804a331b4801d88`; image `sha256:998c5a1e9e5f70f7e09f1f2a8c316bbc3714bd9815e68f6b870130a332542c06` | PASS — clean 24-package image build; installed PLC/BMS/interfaces, IgH 1.6.10, exact PID 1/CMD, single-service Compose and exact capabilities verified; image environment/history contain no proxy; no-device Mock exposes `/tf` and `/tf_static`, keeps PLC/BMS absent, reports expected controller states, accepts idempotent `/rt/disable`, and exits 0 through the ordered shutdown path without `UNCLEAN_SHUTDOWN` | Release/launcher now lock the Stage A source and image identity, but the immutable target release, BMS/PLC read-only checks, three output-point tests and first complete one-command start→READY→stop are not yet verified. ShellCheck is unavailable locally; PLC/BMS sharing CPU14 remains an explicitly accepted temporary risk. |
| T-RT-IO-003 | source: this commit | PASS — target commissioning reproduced a deterministic 100 s stop timeout: both Python nodes raised `ExternalShutdownException`, exited 1 and were respawned during shutdown; three regression tests failed before the fix and pass after it; launch respawn was removed while in-process Modbus/CAN reconnect remains; affected four-package build, 49 package tests, launch parsing, Compose expansion, `git diff --check` and `tools/quality_gate.sh` pass | The failed candidate safely reached EtherCAT Idle/Inactive with all 16 slaves PREOP, but Docker forced exit 137, so it is rejected. A clean corrective image and repeated target start/stop are mandatory before PLC output or full-enable testing. ShellCheck remains unavailable locally. |
| T-RT-IO-004 | source `d415c0c2c75917a9545a4a2f87487718de8622a2`; image `sha256:01bd550b068fccb9158b007067e55c30eed7d7d7253ef9179dfdf6d9be9a11c2` | PASS — clean 24-package corrective image build; image environment/history contain no proxy; a controller-ready no-device Mock with PLC/BMS enabled stopped in 2 s, exited 0 and logged the ordered disable/controller/EtherCAT shutdown markers with no traceback, process death, respawn or `UNCLEAN_SHUTDOWN`; launcher/policy now lock the corrected identity | Target release replacement and repeated target A/B start→stop remain mandatory. PLC output-point tests, HMI comparison and first full one-command enable are still gated by explicit onsite confirmation. ShellCheck is unavailable locally. |
| T-RT-IO-005 | source `d415c0c2c75917a9545a4a2f87487718de8622a2`; lock `7bc8f16e903ef7174f60ebe9613f6eeed3a96185`; evidence: this commit | PASS — target repeated read-only start/stop with PLC/BMS; three approved `SetBool` outputs each changed only its `%MW200/%MW211` target bit and returned to zero; production one-command start returned `/rt/enable ok=true` and READY with no FJT or `/cmd_vel` publisher, then stop returned `/rt/disable ok=true`, container exit 0, EtherCAT Idle/Inactive with 16 PREOP, CAN0/1 zero errors and PLC output registers zero; evidence SHA-256 manifests exist in target runs 20–22 | Independent BMS/HMI visual comparison and onsite physical left/right/common-pump/vacuum-path observation remain; PLC register/actual-output-bit behavior is verified. CPU14 co-location remains a temporary accepted risk, BQ-121 stays open, and ShellCheck is unavailable locally. |
| T-020 (sensor-frame follow-up) | source: this commit; onsite reference SHA-256 `db1b04c9b5aba34a2a9671a0f343d59b9d9dcdad9c9427dae2de2e3881ce8dff` | PASS — test-first contract failed only on the missing perception subtree, then passed 5/5 after adding the MID360 mount/native frames, `livox_fused_frame`, `top_sensor`, rear native frame and camera optical frame at the exact approved transforms; the same test preserves underscored control joints and Updown `0–0.8 m / 0.3 m/s`; Xacro/check_urdf reports one complete tree rooted at `base_footprint`, the shared package builds and `tools/quality_gate.sh` passes 28 tests at 84% policy coverage | A clean image, no-device runtime `/tf_static` queries and production release/launcher lock update remain. The running target container still contains the previous model and must not be hot-patched; switching it requires a separately controlled disable/stop/redeploy/start sequence. Perception/motion consumers must remove duplicate static publishers and validate the changed native lidar and front-camera frame semantics. |
| T-020 (sensor-frame image/lock) | source `4d6d48bb0c621f33edb2e2603c05bf622e5fcec8`; image `sha256:f495d461cddced5c9b1fd5c8d0b3543a4b0c04f9e98b465a79b4d5fde0a0237e`; lock: this commit | PASS — clean Docker build completes all 24 packages with the frozen IgH/ICube/ros2_canopen/JTC inputs; installed Xacro contains the seven new sensor links; hardware-free Domain 142 Mock maps no devices and verifies all nine approved perception transforms numerically on `/tf_static`; local image archive SHA-256 `202cab17db8f6dae6f4af9a8a46f84bf74e13c04176b75a29e3470df39180bd0` loads on the target to the identical image ID; launcher policy and repository quality gate pass | Production start, target `/tf_static` query and consumer-side duplicate-publisher check remain. The prior target container was operator-stopped cleanly before image import; this lock update does not itself authorize or perform `/rt/enable` or motion. |
| T-020 (target sensor-TF deployment) | runtime `4d6d48bb0c621f33edb2e2603c05bf622e5fcec8`; lock `c6f24867a23819e26413aaea7926537e94616a00`; evidence: this commit | PASS — target release/operator archives and the installed model/launcher were SHA-256 verified before atomically moving `rt-control-current`; target no-device Domain 142 Mock published all nine required perception `/tf_static` edges and exited 0; after explicit operator authorization the production launcher passed its host/bus/image gates and `/rt/enable` returned `ok=True, stage=success`; the locked image runs with restart count 0, JSB/JTC/diff-drive/enable-manager are active, `/joint_states` contains 16 joints and all 14 EtherCAT axes, `/tf_static` has one publisher and all nine new edges, and all 17 rt-control diagnostic statuses are OK; no FJT, `/cmd_vel` or PLC output command was sent | `lidar_front/lidar_rear` native-axis and `cam_front` pose semantics are now live public TF contracts; perception/motion must use the same model and remove private duplicate static publishers. No navigation/FAST-LIO/camera consumer integration or physical frame-direction calibration was performed in this rt-control deployment. |
| T-021 | source: this commit | PASS — test-first recovery contract reproduced the missing checker, then 6/6 focused tests pass; the dedicated launcher path preserves ordinary-start no-reset policy, orders best-effort disable/session destruction/operator confirmation/single reset/axis-state proof/single enable, and `bash -n` plus launcher policy pass | No production image/release lock or target file was changed. The new path has not performed a real contactor power-loss/recovery cycle; that remains a separate L3 hardware test. Four Ti5 axes retain the highlighted BQ-115 `0x0021` non-energized exception. |
| T-022 | source: this commit | PASS — test-first TF/odometry contract reproduced the old TF ownership; affected bringup/IO packages build; a hardware-free Domain 143 Mock on the audited production base reports `enable_odom_tf=False`, one `/wheel/odom` publisher at about 50 Hz with `odom/base_footprint`, no old odometry topics, no `odom` parent TF over 6 s, and the unchanged static `base_footprint→base_link` edge at 0.202094 m; container exits 0 | No new production image/release lock or target deployment was made. Navigation has not yet published/validated its final `/odom` and sole `odom→base_footprint`; until it does, the combined `odom→base_link` path is intentionally absent. Existing consumers of `/diff_drive_controller/odom` must migrate. |
| T-021/T-022 (image/release lock) | source `1d4fb230140b555b05c69c6b29529c5137cb136c`; image `sha256:4b78c1b194ff7cbfdcc79672273a11ec3314081774465361a4cb7947ac4d9e75`; lock: this commit | PASS — clean 24-package Docker build and hardware-free Domain 144 Mock passed; the installed runtime exposes only `/wheel/odom`, suppresses the rt-control `odom→base_footprint` TF, retains the approved static model tree, and contains the controlled `recover-power-loss` operator path; launcher identity/policy and repository quality gates are rerun by this lock commit | Target import and atomic release/operator switch remain. No powered recovery cycle, navigation-owned `/odom`/TF integration, automatic enable, FJT, velocity or PLC output command is covered by this lock. |
| T-021/T-022 (unpowered target switch) | runtime `1d4fb230140b555b05c69c6b29529c5137cb136c`; lock `82d5b40a70be9b3712e7590624b8426b7000c89d`; evidence: this commit | PASS — target SHA-256 verified the image/release/operator archives, loaded the exact image ID, expanded the locked Compose configuration, and atomically moved `/home/ar/rt-control-current` from lock `c6f24867…` to `82d5b40…`; the old release and image remain available for rollback | The control power, CANopen nodes and PLC were offline. The reboot-autostarted old container had already lost `ros2_control_node` on CANopen boot timeout, so its bounded stop ended `137`; the new image was intentionally not started and no reset, enable, FJT, velocity or PLC output command was sent. Powered startup, the dedicated recovery cycle and navigation-owned `/odom`/TF integration remain unverified. |
| T-021/T-022 (powered target first start) | runtime `1d4fb230140b555b05c69c6b29529c5137cb136c`; lock `82d5b40a70be9b3712e7590624b8426b7000c89d`; evidence: this commit | PASS — after the operator-authorized `can1.service` restart restored 5 Hz BMS `0x3FC`, the locked ordinary launcher passed its host/bus/image gates and `/rt/enable` returned `ok=True, stage=success`; the exact image runs with restart count 0, JSB/diff-drive/JTC/enable-manager are active, `/joint_states` contains 16 joints, `/wheel/odom` is 50.0 Hz with one publisher, all 14 EtherCAT axes and CANopen nodes 2/3 report level-0 OperationEnabled, PLC is connected/fresh with all outputs off, BMS is present, and both CAN links have zero error counters | The container is intentionally left running and enabled for the operator. No FJT, `/cmd_vel` or PLC output command was sent, and this cycle has not yet exercised the new-image stop path. EtherCAT startup accumulated `wc_error_count=8` before diagnostics settled healthy; the accepted physical-link risk remains. The dedicated contactor power-loss recovery cycle and navigation-owned `/odom`/TF integration remain unverified. |
| T-DEV-NATIVE-001 | source: this commit | PASS — target-locked native launcher explicitly uses Domain 0 with Fast DDS default UDP+SHM and no inherited DDS XML; native bootstrap keeps vendor/build/install/log outside the repository, verifies exact frozen dependency trees, and supports package-select symlink builds; ordinary native start cannot overlap Docker and never resets/enables; 9 focused native contract tests, shell syntax, `git diff --check` and the 43-test `tools/quality_gate.sh` pass at 84% policy coverage | No Docker/Compose configuration, target file, package, bus or container was changed; no native dependency install/build/start/enable or image build was performed. The deployed target lock remains Domain 42 until a separate Docker change and clean image/release switch. Domain 0 widens discovery scope; Fast DDS SHM/QoS/high-bandwidth multi-process stability needs joint validation. ShellCheck is unavailable locally and remains CI-enforced. |
| T-DEV-NATIVE-001 (target source handoff) | source `160869cbaf54534df46234b5a620a335ce0f6952`; bundle SHA-256 `9345ed9f66322cc55d1cd6efff0632e6a059cb73eb7bfc1cb8bf7e3db0debd7c`; evidence: this commit | PASS — exact complete-history bundle cloned as clean branch `feature/rt-control-native-development` at `/home/ar/rt-control-dev/robot` on `ar-Default-string`; target HEAD/branch, bundle integrity, executable bits, `git fsck`, shell syntax and 9/9 native contract tests pass; the updated quick-start guide is present | Source-only handoff: vendor/build/install/runtime directories were confirmed absent; no dependency install, build, bus access, reset, enable or native start occurred. Existing `robot-rt-control-1` was read-only observed `exited` and `/home/ar/rt-control-current` was not modified. The target clone intentionally has no remote; subsequent updates require another verified bundle or an explicitly approved remote policy. |
| T-DEV-NATIVE-001 (target native build) | source `a022b1052e9b76480e8e37e4dac8913a81eba392`; target `/home/ar/rt-control-dev/robot`; evidence: this commit | PASS — target fast-forwarded by verified bundles; `prepare` passes twice with exact frozen vendor trees; `install-deps` reports all rosdeps installed; full native symlink build completes 24 packages in 1 min 47 s; `bootstrap_native_dev.sh doctor`, `bash -n` for native/IgH scripts, 12/12 native contract tests, and installed `ros2 pkg prefix rt_control_bringup enable_manager` checks pass; host IgH identity metadata now matches `versions.env` | No native `start`, reset, `/rt/enable`, FJT, `/cmd_vel`, PLC output, bus restart or Docker release change was performed. Build logs contain non-fatal colcon override warnings for patched controller packages and existing `lely_core_libraries`/`robot_hw_canopen` stderr warnings. Target clone still intentionally has no remote; updates continue by verified bundle unless remote policy is explicitly approved. |
| T-DEV-NATIVE-001 (target native enable) | source `ced5fb0aa389716cbceaf0dc04085375d60bf116`; target `/home/ar/rt-control-dev/robot`; evidence: this commit | PASS — native symlink-installed `rt_control_start` executable-bit defect was fixed and fast-forwarded to target; `rt_control_native.sh doctor` passes; target `start` runs on CPU14 in Domain 0 with Docker exited; explicit `/rt/reset_fault` returns `ok=True, stage=success`; disabled-state check passes for all 14 axes; native `/rt/enable` returns `ok=True, stage=success`; diff-drive, JTC, JSB and enable-manager are active; all 14 EtherCAT axes satisfy OperationEnabled; EtherCAT master is Operation/Active with 16 slaves and CAN0/1 are ERROR-ACTIVE with zero bus/error-pass/bus-off counters; `/cmd_vel` is subscribed by diff-drive and `/wheel/odom` has one diff-drive publisher | Native stack is intentionally left running and enabled for the operator. No FJT, `/cmd_vel`, PLC output or motion command was sent. A direct `start-and-enable` attempt reached READY but called `/rt/enable` while startup sanitize was still in progress, then stopped uncleanly because right_joint2 still required reset; use the explicit start/reset/enable recovery sequence until the native wrapper gains an idle/fault wait gate. EtherCAT lost frames increased 5→6 during startup; the accepted physical-link risk remains. |
| T-DEV-NATIVE-001 (native one-click recovery) | source: this commit | PASS — test-first native recovery contract reproduced the missing one-click entrypoint and reset-capable service wrapper; `rt_control_native_oneclick.sh` now delegates to the explicit `recover-power-loss` path; shell syntax and 15/15 focused native launcher tests pass; `recover-power-loss` preserves ordinary no-reset start/enable, stops stale native sessions without SIGKILL, waits for EtherCAT idle, starts fresh, waits for enable-manager `IDLE`/`fault_requires_reset`, calls one `/rt/reset_fault`, proves disabled state, then calls one `/rt/enable` and proves enabled state | The new one-click recovery path was not executed on powered hardware and no target runtime was restarted in this change. Current target native stack from the previous handoff was intentionally left running/enabled; a real contactor power-loss recovery cycle remains an explicit onsite test. |
| T-DEV-NATIVE-001 (native recovery checker corrective) | source: this commit | PASS — RED reproduced the one-click recovery failure where reset succeeded but `check_axis_states` rejected a readable, non-executable Python helper; the launcher now requires the helper file to exist and be readable, then invokes it through `python3`; focused native tests pass 16/16 with shell syntax clean | The target session that the operator manually recovered remains running/enabled. This corrective sync does not rerun one-click recovery, stop, reset, enable, FJT, `/cmd_vel` or PLC output. |
| T-DEV-NATIVE-001 (dynamic-state YAML preamble corrective) | source: this commit | PASS — RED reproduced the live one-click failure where `ros2 topic echo` printed `A message was lost!!!` before the YAML document, causing the axis-state checker to reject an otherwise valid `/dynamic_joint_states` sample; the checker now strips any preamble before the first `---`; focused recovery/native tests pass 23/23 with shell syntax clean | The target stack was only inspected read-only; it remains running after reset with controllers healthy. This corrective sync does not rerun one-click recovery, stop, reset, enable, FJT, `/cmd_vel` or PLC output. |
| T-DEV-NATIVE-001 (dynamic-state sampling retry corrective) | source: this commit | PASS — RED reproduced the one-click false failure where `/rt/enable` succeeded but a single `ros2 topic echo --once /dynamic_joint_states` sample failed; `check_axis_states` now retries sampling/parsing for 30 seconds before failing; focused native/recovery tests pass 24/24 with shell syntax clean | The target stack was only inspected read-only and is already enabled. This corrective sync does not rerun one-click recovery, stop, reset, enable, FJT, `/cmd_vel` or PLC output. |
| T-DEV-NATIVE-002 (CPU14 scheduler gate) | source: this commit | PASS — test-first native contract added the reviewed CPU-topology gates; focused `python3 -m unittest tools.tests.test_rt_control_native` passes 27/27; `python3 -m py_compile tools/rt_control_thread_affinity.py tools/tests/test_rt_control_native.py`, `bash -n tools/rt_control_native.sh tools/rt_cpu_contamination_check.sh tools/canopen_heartbeat_watch.sh hostsetup/igh-install.sh hostsetup/verify-host.sh`, `tools/rt_cpu_contamination_check.sh --cpu 14 --mode warn --samples 1 --interval 0.01`, `git diff --check`, and a temporary clean HEAD plus this change-set `tools/quality_gate.sh` pass at 84% coverage | No target-host root write, reboot, bus start, `/rt/reset_fault`, `/rt/enable`, FJT, `/cmd_vel` or PLC output was performed. The live dirty worktree still contains unrelated Docker/repository-gate changes, so `tools/quality_gate.sh` on the live tree fails with existing `docker/compose.yaml` policy errors; those unrelated edits are not included in this task. IgH `run_on_cpu=14` requires target module reload/reboot and powered verification. |
| T-DEV-NATIVE-002 (target enable/disable) | source `98e9d12`; evidence: this commit | PASS — target `/home/ar/rt-control-dev/robot` fast-forwarded to `98e9d12`; `/etc/modprobe.d/ec_master.conf` was changed to `options ec_master run_on_cpu=14` and `ethercat.service` restarted; `/sys/module/ec_master/parameters/run_on_cpu=14`; `rt_control_native.sh doctor`, focused native tests, py_compile, bash syntax and CPU14 contamination check pass; native `start` runs outer stack on housekeeping CPUs and pins the unique FIFO80 update thread `tid=7903` to `PSR=14`; `EtherCAT-OP` also runs on `PSR=14`; after one early `/rt/reset_fault` timeout on `right_joint1/status_word=0`, a second group `/rt/reset_fault` returned `ok=True, stage=success`; `/rt/enable` returned `ok=True, stage=success`; all 14 EtherCAT axes satisfy enabled CiA 402 contract, controllers `dual_arm_jtc`, JSB, diff-drive and enable-manager are active, EtherCAT is Operation/Active with 16 slaves and zero lost frames; `rt_control_native.sh stop` returned `/rt/disable ok=True` and exited cleanly; final native state is stopped, EtherCAT Idle/Inactive with all 16 slaves PREOP, can0/can1 ERROR-ACTIVE with zero CAN error counters | No FJT, `/cmd_vel` or PLC output was sent. Startup still produced transient IgH SDO read-only PDO-mapping warnings and `0x001B Sync manager watchdog` recovery messages before final OP; this does not block the completed enable/disable smoke but keeps BQ-120 root-cause/long-window validation open. First reset timeout indicates manual reset immediately after READY can race stale status; the approved recovery script's wait/retry gates remain the safer operator path. |
| T-DEV-NATIVE-002 (unattended one-click hardening) | source: this commit | PASS — test-first native launcher contract reproduced the missing unattended/fault/log-limit behavior, then focused native/recovery tests pass 37/37; `bash -n` passes for `rt_control_native.sh`, `rt_control_native_oneclick.sh` and `rt_cpu_contamination_check.sh`; `py_compile` passes for the touched Python tests/checkers. The one-click `recover-power-loss` path is now unattended, self-raises `rtprio>=98`/`memlock=unlimited` through `sudo prlimit --pid $$`, checks enable-manager fault state, auto-calls one `/rt/reset_fault` only for `fault_requires_reset`, prints reset response/diagnostics and a power-cycle hint when the fault cannot be cleared, aggregates broad CPU14 high-priority affinity warnings into one line, preserves hard blocking only for actual/tight CPU14 realtime contamination, starts the runtime through `setsid` so monitor `Ctrl+C` does not target the control process, and tails only WARN/ERROR-class runtime logs after successful enable. `RT_CONTROL_NATIVE_MONITOR=false` is supported only for automated status handoff | Not yet deployed or run on the target IPC after this code change. Because the successful one-click path now keeps a foreground filtered log monitor by default, automated hardware verification must use `RT_CONTROL_NATIVE_MONITOR=false` followed by a separate status check. No bus start, reset, enable, stop, FJT, `/cmd_vel` or PLC output command was performed by this local change. |
| T-DEV-NATIVE-002 (target unattended one-click handoff) | source `47ecd70`; target `/home/ar/rt-control-dev/robot`; evidence: this commit | PASS — remote `native` was advanced to `47ecd70` and the target source was fast-forwarded by verified bundle; old native runtime first returned `/rt/disable ok=True` and stopped cleanly before source replacement; target `doctor`, bash syntax and 37 focused native tests passed; `RT_CONTROL_NATIVE_MONITOR=false ./tools/rt_control_native_oneclick.sh` ran unattended, emitted one aggregated broad-mask WARN naming ToDesk/pipewire/rtkit threads, pinned the unique FIFO80 update thread to CPU14, observed no `fault_requires_reset`, retried `/dynamic_joint_states` until disabled proof passed, called one `/rt/enable` with `ok=True stage=success`, and proved all 14 axes enabled. Final status: native running in Domain 0, Docker exited, controllers active, enable-manager `ENABLED/success`, EtherCAT Operation/Active with 16 slaves, FIFO80 update and `EtherCAT-OP` on CPU14, can0/can1 ERROR-ACTIVE with zero CAN warning/passive/bus-off counters | Native stack is intentionally left running and enabled for the operator. No FJT, `/cmd_vel`, PLC output or motion command was sent. `Lost frames: 4` after startup keeps the accepted EtherCAT physical-link risk open. One or more `/dynamic_joint_states` CLI samples can still fail transiently before retry success; the launcher retry path handled this. Default human one-click mode still foreground-monitors WARN/ERROR logs; this target verification disabled only the monitor tail so the SSH handoff could complete. |
| T-DEV-NATIVE-002 (CANopen master CPU14 pin) | source: this commit; target `/home/ar/rt-control-dev/robot`; evidence: this commit | PASS — RED contract tests first failed for missing `rtcan-master` gate and ros2_canopen patch; local GREEN: `git -C /home/kkozia/rt_control_refs/ros2_canopen apply --check patches/ros2_canopen/0004-name-canopen-master-loop-thread.patch`, `python3 -m py_compile tools/rt_control_thread_affinity.py`, `bash -n tools/rt_control_native.sh tools/bootstrap_native_dev.sh`, and `python3 -m unittest tools.tests.test_rt_control_native` pass. Target was stopped via `/rt/disable ok=True`, files synced without reset/clean, ros2_canopen 0004 applied, 8 CANopen/bringup packages rebuilt, target focused tests/syntax checks passed, and `RT_CONTROL_NATIVE_MONITOR=false ./tools/rt_control_native_oneclick.sh` recovered to enabled. Runtime proof: `rtcan-master` TID `438999` on CPU14, FIFO80 update TID `442312` on CPU14, `EtherCAT-OP` TID `439209` on CPU14; controllers active, EtherCAT Operation/Active with 16 slaves, can0/can1 ERROR-ACTIVE with zero warning/passive/bus-off counters, and all 14 axes satisfy enabled CiA 402 contract | Native stack is intentionally left running and enabled for the operator. No FJT, `/cmd_vel`, PLC output or motion command was sent. Target `bootstrap_native_dev.sh prepare` is currently blocked by pre-existing ecat_icube vendor tree drift, so this handoff used a minimal ros2_canopen patch/build path; do not reset the vendor tree without separate approval. EtherCAT `Lost frames: 9` and CAN RX dropped counters remain physical/driver-layer observation items, not closed by this thread-affinity change. |
| T-DEV-NATIVE-002 (native/Docker startup parity corrective) | source: this commit | PASS — source synced to target and host CAN install updated: Ti5 `right_joint2`/`left_joint3` now reverse position by master-side software polarity only, negating both `0x607A` command and `0x6064` feedback factors and no longer writing `0x607E`; gs_usb autoload remains, but `rt-control-can-names.service`, `can0.service` and `can1.service` are disabled at boot, while native and Docker launchers both wait for the two fixed CAN adapter serials, force `can0/can1`, apply 500 kbit/s plus txqueuelen 128 and verify UP/ERROR-ACTIVE before launching; native and Docker stop/boot paths detect early `ros2_control_node` exit, print the first boot error and terminate launch/spawner instead of waiting on unavailable `/rt/disable`; Docker source now matches native runtime policy with Domain 0, FastDDS default transport, no CycloneDDS volume, housekeeping launch CPU set, enable-time thread pin for FIFO80 update plus `rtcan-master`, and Dockerfile application of ros2_canopen patch 0004. Verified: bash syntax, 51 focused unittest, 12 focused pytest, compose config, `git diff --check`, IPC launcher policy, and `tools/quality_gate.sh` pass; host systemd reports all three CAN units disabled. | Current native runtime was intentionally left running and was not restarted, so the loaded process still uses the previous configuration until the next controlled restart/rebuild. No new Docker image, release lock, reset, enable, stop, motion, PLC output or CAN reconfiguration was executed after this source change. Docker parity still requires a new commit, clean image build, lock update and controlled target start/stop validation. |
| T-DEV-NATIVE-002 (Docker parity image/release lock) | source `6ab964ed1d260264be93754bd9e412dbba6bd6cc`; image `sha256:b4b0fd8baead8e4bef601010875c3a36bb3ad6a23bf2be064f7e98f2997b7e7b`; evidence: this commit | PASS — clean Docker build completed all 24 packages with Domain 0/FastDDS Docker parity, no CycloneDDS volume, `RT_CONTROL_START_CPUSET` support, ros2_canopen `rtcan-master` thread-name patch, CAN preflight integration, boot/stop early-death handling, and Ti5 software polarity profiles; image inspection confirms no proxy environment variables, installed `rt_control_start` contains the housekeeping CPU-set wrapper, and `right_joint2`/`left_joint3` no longer contain startup `0x607E` writes while command/feedback factors are negated. Launcher identity/policy and repository quality gates are rerun by this lock commit. | Target release switch and powered Docker start/stop validation remain. No reset, enable, stop, FJT, `/cmd_vel` or PLC output command is performed by the image build/lock update itself. If Ti5 drives retain a previously written `0x607E=0x80` until power-cycle, that hardware-state residue is outside this software-only polarity change and must be observed before direction motion tests. |
| T-DEV-NATIVE-002 (reset-ready gate corrective) | source: this commit | PASS — powered Docker recovery exposed a host-script gate bug: `wait_for_enable_manager_reset_ready()` could return on a diagnostic `value: IDLE` before checking a simultaneous non-resettable failure/`restart_required`, so `/rt/reset_fault` was called even though enable_manager was already fail-closed and returned `stage=restart_required`; the failed run exited the container, released EtherCAT to Idle/Inactive with all 16 slaves PREOP, and left CAN0/CAN1 ERROR-ACTIVE. Native and Docker launchers now check `FAILED`/`restart_required` before `IDLE`, only allowing `fault_requires_reset` through; focused launcher tests, shell syntax, `git diff --check` and `tools/quality_gate.sh` pass. | The corrective change is host/operator script only; the already built image `sha256:b4b0fd8baead8e4bef601010875c3a36bb3ad6a23bf2be064f7e98f2997b7e7b` is unchanged. A new operator release and another controlled Docker recovery/start/stop validation are still required. No FJT, `/cmd_vel` or PLC output command was sent. |
| T-DEV-NATIVE-002 (idempotent JTC deactivate corrective) | source: this commit | PASS — powered Docker recovery showed `/rt/reset_fault` could enter the emergency fault-stop path while `dual_arm_jtc` was already inactive; the enable manager then sent a STRICT deactivate, controller_manager rejected the no-op as `no controller is switched`, and enable_manager set `restart_required`. `switchJtc(false)` now queries `list_controllers` first and treats a missing/non-active JTC as an already-safe success while preserving STRICT deactivate when JTC is active. Verified: focused native/recovery tests, enable_manager symlink build, shell syntax, `git diff --check`, and `tools/quality_gate.sh` pass. | This changes image-contained C++ and therefore requires a new Docker image, new source/image/operator lock, and another controlled target Docker recovery/start/stop validation. No FJT, `/cmd_vel` or PLC output command was sent. |
| T-DEV-NATIVE-002 (Docker parity relock after JTC corrective) | source `ad7436b9321d92141899b5dbf2bbb6844df704cb`; image `sha256:685ddc7ac8623403fb42f19db7f71e0a1bbd2fd82a851eabf0494057bc7511d5`; evidence: this commit | PASS — clean Docker rebuild completed all 24 packages with the idempotent JTC deactivate corrective; image inspection confirms installed `enable_manager` is present, source contains the `ListControllers` pre-check and inactive-JTC no-op success path, and Ti5 `right_joint2`/`left_joint3` still do not contain startup `0x607E` writes. Launcher identity/policy and repository quality gates are rerun by this lock commit. | Target release switch and powered Docker recovery/start/stop validation remain. No reset, enable, stop, FJT, `/cmd_vel` or PLC output command is performed by this image build/lock update itself. |
| T-DEV-NATIVE-002 (reset standard disable corrective) | source: this commit | PASS — powered Docker recovery then exposed the next reset-state issue: after fault reset, XMC Updown reported `0x8637` (decoded as OperationEnabled), while updateReset wrote direct `0x0000` and waited for disabled until `fault_reset_timeout`. updateReset now uses the standard CiA 402 down-sequence for non-fault axes during reset (`0x0007` from OperationEnabled, `0x0006` from SwitchedOn, then `0x0000`) instead of one-step zeroing. Verified: focused launcher test, enable_manager symlink build, shell syntax, `git diff --check`, and `tools/quality_gate.sh` pass. | This changes image-contained C++ and therefore requires another Docker rebuild, source/image/operator relock, and controlled target Docker recovery/start/stop validation. No FJT, `/cmd_vel` or PLC output command was sent. |
| T-DEV-NATIVE-002 (native JTC deactivate state gate) | source: this commit | PASS — native oneclick showed startup sanitize could still request emergency JTC deactivate even though enable_manager had never activated `dual_arm_jtc`, producing the same controller_manager STRICT no-op warning while Updown remained at `0x8637` and enable_manager failed `disable_timeout`; the run did not call `/rt/enable` or motion, and native stop released EtherCAT back to Idle/Inactive with all 16 slaves PREOP. enable_manager now tracks whether JTC deactivation is actually required: it sets the flag before a JTC activation attempt, clears it on confirmed deactivate or activation failure, and skips emergency deactivate when startup/reset sanitize never activated JTC. Verified: `colcon build --symlink-install --packages-select enable_manager`, 49 focused native/recovery tests, shell syntax, `git diff --check`, and `tools/quality_gate.sh` pass. | No Docker build/relock was performed per the native-first workflow. A repeat native oneclick recovery is still required; Updown `0x8637` disable behavior remains the live validation target. No FJT, `/cmd_vel` or PLC output command was sent. |
| T-IF-RT-001 (public interface names stage 1) | source: this commit | PASS — first-batch contract alignment keeps chassis command as `geometry_msgs/msg/Twist` with 0.5 s timeout but exposes it on `/cmd_vel_safe`; renames 14-axis JTC to `whole_body_jtc`; restricts `/joint_states` to the 14 EtherCAT mechanical axes at 100 Hz with `position` only; and publishes BMS on `/battery_state` at the approved 5 s period. Verified: `bash -n`, `py_compile`, `git diff --check`, 63 focused unittests including the new public-interface regression, 13 BMS pytest tests, direct YAML/interface assertions, and `tools/quality_gate.sh` pass. Isolated local `colcon` builds pass for `robot_interfaces`, `bms_node`, and `enable_manager`; `rt_control_bringup` install validation is blocked on this local workstation by missing `lely_core_libraries` through the declared `robot_hw_canopen` runtime dependency. | `/control/set_enabled`, vacuum action/state, `/control/safety_state`, and `/rt_control/readiness` remain pending explicit public-adapter/message semantics. No Docker build, remote push, bus start, reset, enable, motion, or PLC output command was performed. |
| T-IF-RT-002 (public set-enabled adapter) | source: this commit | PASS — added temporary `alfa_control_interfaces/srv/SetControlEnabled` and `control_api_adapter` node exposing `/control/set_enabled`. `enabled=true` waits for enable_manager readiness, automatically calls one `/rt/reset_fault` only for `fault_requires_reset`, then calls `/rt/enable`; `restart_required` or reset failure is rejected with structured error text. `enabled=false` only calls `/rt/disable` and does not stop launch, container or master processes. Launch, native bootstrap, Docker package list, IPC readiness checks, docs and repository policy were updated. Verified: RED adapter contract test failed before implementation; GREEN `bash -n`, `py_compile`, 70 focused unittests, 6 package pytest tests, 84-test `tools/quality_gate.sh`, isolated colcon build for `alfa_control_interfaces`, `robot_interfaces`, `control_api_adapter`, `bms_node`, `enable_manager`, and colcon test for `alfa_control_interfaces`, `control_api_adapter`, `bms_node` passed; `ros2 interface show` and `ros2 pkg executables` confirmed the generated service and executable. | This is a native/source change only. No Docker build, remote push, target sync, bus start, reset, enable, motion or PLC output command was performed. Vacuum action/state, `/control/safety_state`, and `/rt_control/readiness` remain pending separate semantics decisions. |
| T-IF-RT-003 (vacuum/status/readiness public adapters) | source: this commit | PASS — added temporary public `VacuumGrip` action, `VacuumState`, `SafetyState`, `SetPumpEnabled` and `DomainReadiness` contracts plus `vacuum_adapter`/`rt_status_adapter` nodes in `control_api_adapter`. Vacuum channels are fixed to `left`/`right`; PLC vacuum sensing remains the approved DI `bit0/bit1` boolean decode; no `pressure_pa` is exposed. `GRIP` currently accepts only `grip_profile_id=default`, turns on the common pump and requested valves, then verifies selected channels by fresh `attached=true`; `RELEASE` turns off selected valves and reports `UNVERIFIED`. Safety/readiness minimally aggregate enable-manager diagnostics, EtherCAT master/slaves, CANopen node2/3, PLC freshness and BMS freshness. Verified: RED contract tests reproduced missing interfaces/adapters; GREEN `py_compile`, 61 focused pytest/unittest cases, 6-package colcon build, 6-package colcon test with 66 tests, `ros2 interface show`, `ros2 pkg executables`, `git diff --check` and `tools/quality_gate.sh` pass. | This is a native/source change only. No Docker build, remote push, target sync, bus start, reset, enable, motion or PLC output command was performed. `rt_control_bringup` isolated install build was not claimed because this local minimal prefix lacks the declared robot_hw/vendor dependency closure; launch is covered by syntax and contract tests. Full versioned grip-profile registry and cross-domain consumer runtime validation remain pending. |
| T-IF-RT-003 (Docker release candidate lock) | source `ff730e3bc51e726f95ffd402c1793b114e41733a`; image `sha256:02ef48587b51bd4ab261f56e71c5274e4a95726f04587dd41176fcb08e7f4beb`; evidence: this commit | PASS — clean Docker build completed 27 packages with the new `alfa_system_interfaces`, `vacuum_adapter`, `rt_status_adapter`, `/control/set_enabled`, `/cmd_vel_safe`, `/wheel/odom`, TF ownership and native/Docker startup parity. First build attempt failed only at GitHub dependency fetch; rerun with one-shot `RT_CONTROL_BUILD_PROXY=http://127.0.0.1:10808` succeeded. Image inspection confirms tag `rt-control:ff730e3bc51e726f95ffd402c1793b114e41733a`, IgH commit `2f7f884f1c7d377c02a7d627eb06512126a0e50e`, no proxy runtime environment, and no concrete proxy URL in image history. | Target release switch and powered Docker start/enable/stop validation remain pending at the time of this lock. Build stderr contained the existing lely setup warnings and robot_hw_canopen DCF warnings (`DynamicChannels`, `HighLimit overflow in [607E]`); no Docker image test sent reset, enable, FJT, `/cmd_vel_safe` or PLC output. |
| T-IF-RT-003 (target Docker sync gate) | source `ff730e3bc51e726f95ffd402c1793b114e41733a`; lock `ba307033d25d6000ab6872d3aae8020d31909396`; image `sha256:02ef48587b51bd4ab261f56e71c5274e4a95726f04587dd41176fcb08e7f4beb`; evidence: this commit | PARTIAL PASS — remote branch `native/interface-v3.2-rt-control` was pushed; target `ar@192.168.0.40` received runtime/operator/image archives, local SHA256 and gzip checks passed, Docker loaded the locked image with the expected ID, `/home/ar/rt-control-current` was switched to `/home/ar/rt-control-operators/ba307033d25d6000ab6872d3aae8020d31909396/robot`, and Compose expansion shows host network, Domain 0, FastDDS, `RT_CONTROL_START_CPUSET=0,2,4,6,8,10,12,16-27` and container cpuset `0,2,4,6,8,10,12,14,16-27`. | BLOCKED — powered Docker enable validation was not run because target preflight showed EtherCAT master Idle/Inactive but `Link: DOWN` and `Slaves: 0` on MAC `8c:59:3c:14:ff:d3`; Docker/containerd/EtherCAT services were active, so this is a现场 EtherCAT link/power/cable prerequisite failure, not an image identity failure. No reset, enable, stop, FJT, `/cmd_vel_safe`, or PLC output command was sent. |
| T-IF-RT-003 (CAN adapter swap corrective) | source: this commit; image unchanged `sha256:02ef48587b51bd4ab261f56e71c5274e4a95726f04587dd41176fcb08e7f4beb` | PASS — target retry exposed the exact boot-order case the CAN preflight was meant to handle: the two approved gs_usb adapters appeared as `can0=BMS` and `can1=rt-control`. `rt-control-can-names.sh` now allows reserved names only when occupied by either approved serial, rejects unknown serials, and then renames through temporary names before forcing `can0/can1` to their approved roles. Verified: `bash -n hostsetup/rt-control-can-names.sh`, focused `NativeHostSetupContractTest`, `git diff --check`, and `tools/quality_gate.sh` pass. | Operator-only change; no Docker image rebuild is required because the runtime image is unchanged. Target operator resync and powered Docker start/enable/stop validation remain pending. The failed retry did not start Docker and did not send reset, enable, FJT, `/cmd_vel_safe`, or PLC output. |
| T-IF-RT-003 (CAN preflight settle corrective) | source: this commit; image unchanged `sha256:02ef48587b51bd4ab261f56e71c5274e4a95726f04587dd41176fcb08e7f4beb` | PASS — target retry then showed `can1` became `UP/ERROR-ACTIVE/500k/qlen128` immediately after the script failed, so the startup gate was racing the gs_usb link-state transition after `ip link set up`. `verify_can_interface()` now retries the post-configuration UP/ERROR-ACTIVE/bitrate/queue checks for up to 5 s while still failing serial mismatches immediately. Verified: `bash -n hostsetup/rt-control-can-names.sh`, focused `NativeHostSetupContractTest`, `git diff --check`, and `tools/quality_gate.sh` pass. | Operator-only change; no Docker image rebuild is required. Target operator resync and powered Docker start/enable/stop validation remain pending. The failed retry still did not start Docker and did not send reset, enable, FJT, `/cmd_vel_safe`, or PLC output. |
| T-IF-RT-003 (controller readiness settle corrective) | source: this commit; image unchanged `sha256:02ef48587b51bd4ab261f56e71c5274e4a95726f04587dd41176fcb08e7f4beb` | PASS — powered Docker retry reached controller-manager service discovery but failed before enable because spawners activated `enable_manager` shortly after the instantaneous pre-enable controller check. Docker IPC and native launchers now wait up to 30 s for `joint_state_broadcaster`, `diff_drive_controller` and `enable_manager` active plus `whole_body_jtc` inactive before proceeding. Verified: `bash -n tools/rt_control_ipc.sh tools/rt_control_native.sh hostsetup/rt-control-can-names.sh`, 52 focused launcher tests, `git diff --check`, and `tools/quality_gate.sh` pass with 94 tests. | Operator-only change; no Docker image rebuild is required. The failed retry stopped before `/rt/enable`, but its incomplete-start cleanup logged `UNCLEAN_SHUTDOWN` because shutdown interrupted a not-yet-complete startup; controlled start/enable/stop validation is still pending. No FJT, `/cmd_vel_safe`, or PLC output command was sent. |
| T-IF-RT-003 (native status adapter corrective) | source: this commit | PASS — native target test on `e5bcd6c` reached `/rt/reset_fault ok=True stage=success` and `/rt/enable ok=True stage=success`, but exposed two native handoff defects: `rt_status_adapter` crashed on Python `DiagnosticStatus.level=b'\\x03'`, and oneclick tried to pin the FIFO80 update thread before controller-manager had spawned it. The adapter now converts byte diagnostic levels safely, and native start no longer pins before controller readiness; enable remains hard-gated by thread pin. Native oneclick now validates reset/enable completion through enable_manager diagnostics (`IDLE/success`, `ENABLED/success`) instead of the deprecated public `/dynamic_joint_states/status_word` path. Verified: `py_compile`, focused status-adapter pytest, native/recovery launcher unittests, shell syntax and `tools/quality_gate.sh` pass. | Target source sync, native rebuild and repeat oneclick enable validation are pending. No Docker image rebuild/relock was performed; Docker runtime image still contains the pre-fix status adapter until the next approved release build. |
| T-IF-RT-003 (diagnostic state split corrective) | source: this commit | PASS — RED contract test reproduced that public `/joint_states` contraction left `rt_diagnostics` without `status_word`/EtherCAT synthetic state and caused readiness stale. Added private `rt_internal_state_broadcaster` on `/rt_internal_state_broadcaster/dynamic_joint_states`, disabled public JSB dynamic output, pointed `rt_diagnostics` and native/Docker axis-state checkers at the private topic, and quiesced the internal broadcaster during shutdown. Verified: focused RED/GREEN test, affected-package symlink builds/tests, shell syntax, `check_ecat_sync_shutdown_policy.sh`, `git diff --check`, and `tools/quality_gate.sh` pass. | Native and Docker powered validation are explicitly approved next. Local Mock confirmed the internal broadcaster loads, but full local Mock launch is limited by the workstation missing the `diff_drive_controller` plugin. No FJT, `/cmd_vel_safe` or PLC output command was sent. |
| T-IF-RT-003 (target native validation attempt) | source `648cb391cf604bca1fbb683d712b35c736d4fac6`; target `/home/ar/rt-control-dev/robot` | BLOCKED — target fast-forwarded to `648cb39`; package-select build of `rt_diagnostics enable_manager rt_control_bringup` passed; focused pytest/unittest, shell syntax and EtherCAT shutdown policy passed. Powered native one-click started the stack but did not call `/rt/enable` because the automatic `/rt/reset_fault` failed on `updown` with `status_word=34359 (0x8637)` and `stage=fault_reset_timeout`; the script then fail-closed and stopped the native runtime. Final readback: native stopped, Docker exited, EtherCAT master Idle/Inactive with 16 PREOP slaves and Link UP, can0/can1 UP ERROR-ACTIVE 500 kbit/s qlen 128. | Docker release/build/test is intentionally not started because native-first release criteria were not met. Requires现场主接触器断电复位或 separate approved low-level updown disable investigation before repeating native validation. No FJT, `/cmd_vel_safe` or PLC output command was sent. |
| T-IF-RT-003 (reset staged-disable corrective) | source: this commit | PASS — single-axis SDO evidence showed XMC Updown accepts standard CiA402 down transitions, while `/rt/reset_fault` still used its own compressed reset/zero path. `enable_manager` now treats fault reset as two steps: first pulse `0x0080` only for axes still in Fault, then after all fault targets leave Fault it reuses the existing staged down-sequence (`0x0007 -> 0x0006 -> 0x0000`) through `Phase::kResetDisabling` before reporting `/rt/reset_fault` success. Verified: RED/GREEN focused contract test, 53 focused native/recovery unittests, shell syntax, `git diff --check`, `colcon build --symlink-install --packages-select enable_manager`, `colcon test --packages-select enable_manager` with 199 tests, and `tools/quality_gate.sh` pass. | Local/source validation only. Target sync, native rebuild and powered one-click reset/enable/stop validation remain pending before Docker release. No reset, enable, disable, FJT, `/cmd_vel_safe`, or PLC output command was sent by this local change. |
| T-DEV-NATIVE-002 (CPU14 rcub gate corrective and target oneclick) | source: this commit; target `/home/ar/rt-control-dev/robot` | PASS — target oneclick first failed before bus start because `rt_cpu_contamination_check.sh --mode strict` treated kernel `rcub/2` (`SCHED_FIFO rtprio=1`) as a CPU14 contamination violation. The guard now whitelists `rcub/*` together with existing low-level kernel CPU threads while preserving hard blocking for non-whitelisted realtime threads actually running on CPU14 or tightly affined to CPU14; focused unittest and shell syntax pass locally and on target. After syncing the script, native oneclick completed CAN preflight, started rt-control, auto-called `/rt/reset_fault ok=True stage=success`, pinned `rtcan-master` and FIFO80 update to CPU14, called `/rt/enable ok=True stage=success`, and reported `RECOVERED`. Post-checks show `/rt_control/readiness ready=true`, all controllers active, EtherCAT Operation/Active with 16 slaves, CAN0/CAN1 `UP/ERROR-ACTIVE/500 kbit/s/txqueuelen 128`, and CPU14 `/proc/stat` 5-sample busy rate only 3–5%. | Native stack is intentionally left running and enabled for the operator. No FJT, `/cmd_vel_safe`, PLC output or vacuum action was sent. One previous residual runtime lacked a PID/session handoff and required manual `/rt/disable` plus process-group cleanup; script lifecycle cleanup should be reviewed separately. EtherCAT lost frames remain at the existing accepted physical-link observation level. |
| T-IF-RT-004 (contract 0.5.0 and interface ownership split) | implementation `e66c248`; contract `698d520c6eebaa1437f2a03ca7ff95d95ad3a600` | PASS — the external API now uses the contract-owned `robot_control_interfaces` and `robot_system_interfaces`, while private `/plc/io_state` and `/rt/enable` types live only in `rt_control_interfaces`. Contract 0.5.0 and the main implementation agree on the four adjudicated schemas, 100 Hz `/joint_states`, Autonomy-only `/vacuum/state`, no Perception `/tf` input to RT-Control, and independent `/tf_static` registration. `src/interfaces/source-lock.yaml` pins the authority commit and both public package mirrors are byte-for-byte identical to it. Verified: all 6 contract packages build; contract closure/error-code/changelog/generated-view gates and 31 tests pass; 6 affected RT packages build and their 52 tests pass; `tools/quality_gate.sh` passes 95 tests at 85% policy coverage; `git diff --check` passes. | This is not yet a cross-domain release. The breaking package rename requires every consumer to migrate atomically to contract `698d520`. A full bringup build on this workstation remains blocked by missing `lely_core_libraries`; no Docker image was built for this commit. The configured 100 Hz `/joint_states` rate still requires target runtime measurement because a 250 Hz controller-manager loop may quantize update periods. No target sync, bus access, reset, enable, motion, or PLC output command was performed. |
| T-IF-RT-004 (Docker release lock) | source `2f8e9a24fdfcf368e3effc3a0fcb30733423e16d`; image `sha256:68a4239e7e395cd352f6ed26d9bfe8fceb2209f1e1808f7b28961010f3b19477`; evidence: this commit | PASS — clean Docker rebuild completed all 27 packages after the contract 0.5.0 merge and CPU14 `rcub/*` guard corrective. First no-proxy attempt was interrupted after the upstream fetch layer stalled; rerun with one-shot `RT_CONTROL_BUILD_PROXY=http://127.0.0.1:10808` succeeded. Image inspection confirms tag `rt-control:2f8e9a24fdfcf368e3effc3a0fcb30733423e16d`, IgH commit `2f7f884f1c7d377c02a7d627eb06512126a0e50e`, installed `rt_control_start`, `control_api_adapter` executables, `robot_control_interfaces`/`robot_system_interfaces`/`rt_control_interfaces`, no proxy runtime environment and no proxy URL in image history. Launcher identity/policy and repository quality gates are rerun by this lock commit. | Target release switch and powered Docker start/enable/stop validation remain pending. Build stderr contained the existing lely setup warnings and robot_hw_canopen DCF warnings (`DynamicChannels`, `HighLimit overflow in [607E]`). No container was started, and no reset, enable, disable, FJT, `/cmd_vel_safe`, PLC output or vacuum action was sent by this image build/lock update. |
| T-IF-RT-005 (contract 0.6.0 ErrorInfo adoption) | contract `e19d1450339d6bce598f664eb18fb093e02097ff`; implementation `0128650cd2f028053f22873fdd5ba3cd94a9596c` | PARTIAL PASS — the public provider package is now `robot_rt_control_interfaces`; `SetControlEnabled.Response`, `SetPumpEnabled.Response` and `VacuumGrip.Result` use one `robot_system_interfaces/ErrorInfo`. The non-realtime `control_api_adapter` maps internal enable/PLC results to contract DREE codes and derives `retryable`; enable_manager, private `/rt/*`, PLC services and the 250 Hz loop remain unchanged. Both public mirrors are byte-for-byte identical to the locked contract. Verified locally: 45 focused tests, generated IDL inspection/population, 6 directly affected package builds, 53 accumulated colcon test cases, `git diff --check`, and `tools/quality_gate.sh` with 95 policy tests at 85% coverage pass. Incremental image `sha256:d2fc19c741e6b50304f418a7cb4411f01564f4895b864346f506de2cfd5bd326`, based on the existing `main@2f8e9a2` image, rebuilt the three changed packages in-container, passed 19 package tests and exposed all three `ErrorInfo` contracts in a `--network none` container. | Wire-breaking package/result changes require RT-Control, Motion, Autonomy and tools to pin the same contract SHA for an atomic release. The host-only full `rt_control_bringup` closure remains blocked by missing `lely_core_libraries`. A clean Dockerfile build for `0128650` was attempted but stopped after the GitHub `ros2_canopen` fetch remained hung for more than five minutes; the incremental image is review evidence, not a release image, so this branch is not ready to merge into `main` until a clean build succeeds. No target sync, bus access, reset, enable, motion, PLC output or vacuum command was performed. |
| T-REL-010 (V0.10 release artifact policy) | source: this commit | PENDING — Docker IPC startup no longer hard-locks a Git SHA or Docker image ID in the repository. The approved runtime reference is now release version `V0.10`, image tag `rt-control:V0.10`, target operator root `/home/ar/rt-control-releases/V0.10/robot`, and GitHub Release attachments `rt-control-V0.10-<git-sha>.tar.gz` plus `.sha256`. `rt_control_compose.sh` still defaults Git worktrees to the current full Git SHA for development builds, but accepts explicit release tags for published exports. | Clean Docker build, image export, Release upload, tag creation and target powered validation are pending at the time of this source-policy change. No container was started, and no reset, enable, disable, FJT, `/cmd_vel_safe`, PLC output or vacuum action was sent. |
| Robot Model base-footprint offset follow-up | source: this worktree; target `/home/ar/rt-control-dev/robot` | PASS — relative to `main`, the fixed joint remains `base_footprint -> base_link` and its stored x changes from `0 m` to `+0.15 m`; this includes the previously tested `+0.10 m` intermediate adjustment and places `base_footprint` exactly `-0.15 m` in the `base_link` frame. Updated the TF contract test, passed 5/5 focused tests, package-select symlink build of `robot_description`, Xacro expansion, `check_urdf`, source/install/expanded inverse-transform assertions, unique-source check and repository gate. A later external native restart at 2026-08-13 15:29 loaded the installed model; read-only `tf2_echo base_link base_footprint` and the live `robot_description` parameter both confirm `-0.15 m`. | No bus/controller lifecycle action, reset, enable, disable, motion or Docker build was performed by this task. Docker release evidence remains pending under the separately controlled release workflow. |

## 时间线索引（2026-08-13 起）

以上表格为历史段，自 2026-08-13 起冻结，不迁移、不修改。此后每次变更事件在
`docs/areas/<功能区>/records/` 留完整记录，本节仅追加一行索引：
`日期 | 功能区 | 记录 | verified`。

| 日期 | 功能区 | 记录 | verified |
| --- | --- | --- | --- |
| 2026-08-13 | governance | [建立功能区开发记录体系](docs/areas/governance/records/2026-08-13-establish-area-record-system.md) | UNVERIFIED |
| 2026-08-13 | governance | [首版发布前测试用例目录](docs/areas/governance/records/2026-08-13-test-case-catalog-v1.md) | UNVERIFIED |
| 2026-08-13 | governance | [文档矛盾修正与重复测试清理](docs/areas/governance/records/2026-08-13-doc-contradiction-and-test-dedup.md) | PASS |
| 2026-08-13 | governance | [ELECTRI-89 中低优先清理](docs/areas/governance/records/2026-08-13-historical-annotations-and-nav-cleanup.md) | PASS |
| 2026-08-13 | governance | [修复门禁测试收集缺口并修订覆盖率口径](docs/areas/governance/records/2026-08-13-pytest-collection-and-coverage-scope.md) | PASS |
| 2026-08-13 | governance | [首版测试 runner、JSON 报告与 baseline v0 登记](docs/areas/governance/records/2026-08-13-release-test-runner-v1.md) | PASS |
| 2026-08-13 | governance | [BQ-134 性能阈值裁决落地](docs/areas/governance/records/2026-08-13-bq134-thresholds.md) | PASS |
| 2026-08-13 | contract | [/joint_states 频率裁决为实测 125 Hz](docs/areas/contract/records/2026-08-13-joint-states-125hz.md) | PASS |
| 2026-08-13 | governance | [增量测试 scope resolver](docs/areas/governance/records/2026-08-13-scoped-tests.md) | PASS |
| 2026-08-14 | governance | [T2 性能采集脚本化](docs/areas/governance/records/2026-08-13-rt-perf-capture.md) | PASS |
| 2026-08-14 | lifecycle | [/rt/disable 日志取证：症状真实、机制误诊](docs/areas/lifecycle/records/2026-08-14-rt-disable-log-forensics.md) | PASS |
| 2026-08-14 | lifecycle | [enable_manager 状态机表驱动 gtest](docs/areas/lifecycle/records/2026-08-14-enable-manager-gtest.md) | PASS |
| 2026-08-14 | contract | [公共接口改为固定 SHA vendoring 并采用命名 QoS](docs/areas/contract/records/2026-08-14-robot-interfaces-vendoring-and-qos.md) | PASS（合并受 BQ-137 阻塞） |
| 2026-08-14 | governance | [清空根 docs 并建立公共接口 vendor 门禁](docs/areas/governance/records/2026-08-14-root-docs-and-vendor-governance.md) | PASS |
| 2026-08-16 | contract | [BQ-137 跨域错误与 readiness 语义裁决落地](docs/areas/contract/records/2026-08-16-bq137-shared-error-readiness.md) | PASS（发布待 PR #6 最终 SHA 与全域迁移） |
| 2026-08-17 | contract | [robot_interfaces 回填最终 main SHA](docs/areas/contract/records/2026-08-17-robot-interfaces-final-main-pin.md) | PASS（T0；发布仍待镜像与全域同 SHA smoke） |
| 2026-08-17 | io-power | [纠正左右电磁阀输出映射](docs/areas/io-power/records/2026-08-17-correct-solenoid-side-mapping.md) | PASS（T4；输入映射与新镜像待验证） |
| 2026-08-17 | contract | [Native QoS 运行依赖闭包与 fail-closed 启动门禁](docs/areas/contract/records/2026-08-17-native-qos-runtime-closure.md) | PASS（T3；最终 stopped/Idle/PREOP） |
| 2026-08-19 | contract | [ELECTRI-102 rolling 公共接口所有权裁决](docs/areas/contract/records/2026-08-19-electri-102-public-interface-ownership.md) | UNVERIFIED（T0；接口 PR 等完整 mock/fake 门） |
